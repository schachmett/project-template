# {{ project_name }}
{% if author_name %}
Author: {{ author_name }}
{% endif %}
{% if license == 'mit' %}
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
{% elif license == 'agpl' %}
[![License: AGPL v3](https://img.shields.io/badge/License-AGPL_v3-blue.svg)](LICENSE)
{% elif license == 'proprietary' %}
**Proprietary — all rights reserved.**
{% endif %}

## Quickstart

<!-- TODO: replace with project-specific steps -->
{% if python == 'root' %}
```bash
uv sync
just fix    # format + auto-fix imports
just check  # full CI suite
```
{% elif python == 'component' %}
```bash
cd {{ component_dir }} && uv sync
just fix    # format + auto-fix imports
just check  # full CI suite
```
{% endif %}

## Development

See [AGENTS.md](AGENTS.md) for the full agent workflow, documentation index, and CLI reference.
{% if python != 'none' %}

### Prerequisites

- [uv](https://docs.astral.sh/uv/) ≥ 0.5
- [just](https://just.systems/) (task runner)
- Python {{ python_version }} (managed by uv via `.python-version`)

### Common commands

| Command | Description |
|---------|-------------|
| `just fix` | Auto-format and fix imports (run before committing) |
| `just lint` | Ruff + pyright only (fast feedback, no tests) |
| `just check` | Full CI suite: format-check + lint + typecheck + tests |
| `just --list` | Show all available recipes |
{% endif %}
{% if has_api %}

### Running locally

```bash
just up    # starts docker compose stack
```
{% endif %}
