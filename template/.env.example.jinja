# Copy this file to .env and fill in real values.
# .env is gitignored — never commit it.

# Example:
# DATABASE_URL=postgres://user:password@localhost:5432/{{ package_name | default(project_name) }}
# SECRET_KEY=change-me
