# Operations

<!-- This file is only generated when has_api=true -->

## Environments

| Environment | URL | Purpose |
|-------------|-----|---------|
| production | <!-- TODO --> | Live traffic |
| staging | <!-- TODO --> | Pre-release validation |

## Infrastructure

<!-- TODO: describe infrastructure (cloud provider, services, networking) -->

## Deployment

Releases are triggered by pushing a version tag (see [agent-workflow.md](agent-workflow.md)).

The CD pipeline:
1. Builds the Docker image
2. Pushes to the container registry
3. <!-- TODO: add deploy step (kubectl apply, docker compose pull, etc.) -->
{% if secrets == 'sops' %}

## Secrets management

Secrets are encrypted with SOPS. To work with encrypted files:

```bash
# Edit an encrypted file
sops secrets/production.yaml

# View decrypted (without editing)
sops -d secrets/production.yaml
```

### Key setup

<!-- TODO: document which age/GPG key is required and how to obtain it -->
{% elif secrets == 'env-only' %}

## Environment variables

Copy `.env.example` to `.env` and fill in values. Production values are injected by the deployment platform.

See `.env.example` for the full list of required variables.
{% endif %}

## Docker image

{% if platform in ('forgejo', 'both') %}
**Forgejo registry:** `{{ forgejo_url | replace('https://', '') | replace('http://', '') }}/<org>/{{ project_name }}:<tag>`
{% endif %}
{% if platform in ('github', 'both') %}
**GitHub registry:** `ghcr.io/<org>/{{ project_name }}:<tag>`
{% endif %}

```bash
# Pull latest
docker pull <registry>/<org>/{{ project_name }}:latest

# Run locally
docker run --env-file .env <registry>/<org>/{{ project_name }}:latest
```

## Health checks

<!-- TODO: document health check endpoint and expected response -->

## Logging

<!-- TODO: describe logging approach (structured JSON, log aggregator, etc.) -->

## Monitoring

<!-- TODO: describe monitoring/alerting setup -->
